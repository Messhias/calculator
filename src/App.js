import React from 'react';
import { View } from 'react-native';

import { Header, Panel, Result } from './Components';

const app = () => (
     <View>
        <Header />
        <Result />
        <Panel />
    </View>
);

export default app;
